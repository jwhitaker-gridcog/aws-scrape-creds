# aws-scrape-creds

an overengineerd `wl-paste`. Usage: go to AWS web console, open Web CLI, run `aws configure export-credentials --format=process`, copy+paste into this when you run aws cli commands locally.